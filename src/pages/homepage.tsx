import React from "react";
import Header from "../components/header";
const Homepage = () => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
};

export default Homepage;
